package com.example.servicedeskapk.ui.theme

import androidx.compose.ui.graphics.Color

// Основная палитра — университетская сине-голубая тема
val Primary = Color(0xFF1565C0)          // Глубокий синий
val PrimaryLight = Color(0xFF1E88E5)     // Светлый синий
val PrimaryDark = Color(0xFF0D47A1)      // Тёмный синий
val Secondary = Color(0xFF00897B)        // Бирюзовый
val SecondaryLight = Color(0xFF4DB6AC)   // Светло-бирюзовый
val Background = Color(0xFFF5F7FA)       // Светло-серый фон
val Surface = Color(0xFFFFFFFF)          // Белый
val Error = Color(0xFFD32F2F)            // Красный

// Цвета приоритетов
val PriorityUrgent = Color(0xFFD32F2F)   // Красный — срочно
val PriorityHigh = Color(0xFFF57C00)     // Оранжевый — высокий
val PriorityMedium = Color(0xFFFBC02D)   // Жёлтый — средний
val PriorityLow = Color(0xFF4CAF50)      // Зелёный — низкий

// Цвета статусов
val StatusOpen = Color(0xFF1E88E5)       // Синий — открыта
val StatusInProgress = Color(0xFFF57C00) // Оранжевый — в работе
val StatusWaiting = Color(0xFFFBC02D)    // Жёлтый — ожидание
val StatusClosed = Color(0xFF4CAF50)     // Зелёный — закрыта
val StatusReopened = Color(0xFF7B1FA2)   // Фиолетовый — переоткрыта
val StatusSupport = Color(0xFF00897B)    // Бирюзовый — стор. специалист

// Тёмная тема
val PrimaryDarkTheme = Color(0xFF90CAF9)
val SecondaryDarkTheme = Color(0xFF80CBC4)
val BackgroundDark = Color(0xFF121212)
val SurfaceDark = Color(0xFF1E1E1E)
val SurfaceVariantDark = Color(0xFF2D2D2D)
